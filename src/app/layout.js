import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Rehan's Portfolio",
  description: "Rehan's personal portfolio showcasing projects and skills.",
  keywords: "Rehan Raza, Portfolio, Full Stack Developer, Web Development, React, Next.js, JavaScript",
  authors: [{ name: "Rehan Raza", url: "https://github.com"}],
  creator: "Rehan Raza",
  icons: {
    icon: "/favicon.png",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
